# Baseline Models

Classic time-series models (ARIMA, GARCH) and statistical baselines. Use these for sanity checks and as a conservative benchmark.

Keep training scripts separate and store model artifacts with metadata (dataset hash, params, metrics).
