# ML Models

Tree-based models and gradient boosters (XGBoost, LightGBM, RandomForest). These are typically the first productionized models due to speed and interpretability.

Include training scripts, hyperparameter configs, and small grid-search utilities.
