import React from 'react'

// Compatibility wrapper for tests that import ./Dashboard.jsx
// If you later have a Dashboard.tsx, consider re-exporting from it instead.
export default function Dashboard() {
  return <div>Dashboard</div>
}
