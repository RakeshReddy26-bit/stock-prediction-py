# Frontend environment variables example (Vite)
# When running the frontend dev server set VITE_BACKEND_URL so the dev proxy
# points to the backend you want to use.
VITE_BACKEND_URL=http://localhost:5002

# Other Vite env vars
# VITE_API_KEY=your_api_key
# VITE_SOME_FLAG=false
