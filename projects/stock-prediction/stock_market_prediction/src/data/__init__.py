"""
Placeholder package for data modules.
Some projects include a `data` package. If real data code exists elsewhere, replace this file
or add the real package files. This placeholder prevents editable-install build errors when
`setup.cfg` expects packages under `src/`.
"""
